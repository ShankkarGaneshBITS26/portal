import { PhonenumbervalidationDirective } from './phonenumbervalidation.directive';

describe('PhonenumbervalidationDirective', () => {
  it('should create an instance', () => {
    const directive = new PhonenumbervalidationDirective();
    expect(directive).toBeTruthy();
  });
});
